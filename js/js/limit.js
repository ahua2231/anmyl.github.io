function copyw1() {
    var name = '您好，由于您在挂单出售时，买家支付成功为您上传支付成功截图后，您未在规定时间内【确认放币】导致系统锁定订单，因此被系统禁止出售24小时！';
    var url = '';
    var form = '';
    var content = name.concat(url, form);
    // 使用textarea支持换行，使用input不支持换行
    const textarea = document.createElement('textarea');
    textarea.value = content;
    document.body.appendChild(textarea);
    textarea.select();
    if (document.execCommand('copy')) {
        document.execCommand('copy');
        //alert(content);
        console.log("复制成功");
    }
    document.body.removeChild(textarea);
}
function copyw2() {
    var name = '您好，经核实您在交易期间收到买家款项后没有及时放币，因此触发了系统机制限制了C2C出售权限~';
    var url = '';
    var form = '';
    var content = name.concat(url, form);
    // 使用textarea支持换行，使用input不支持换行
    const textarea = document.createElement('textarea');
    textarea.value = content;
    document.body.appendChild(textarea);
    textarea.select();
    if (document.execCommand('copy')) {
        document.execCommand('copy');
        //alert(content);
        console.log("复制成功");
    }
    document.body.removeChild(textarea);
}
function copyw3() {
    var name = '您好，因您本月第一次超时放币，因此客服提交系统为您解除限制，请您后续交易期间收到买家款项第一时间为买家放币，避免再次被系统禁止账号出售权限~';
    var url = '';
    var form = '';
    var content = name.concat(url, form);
    // 使用textarea支持换行，使用input不支持换行
    const textarea = document.createElement('textarea');
    textarea.value = content;
    document.body.appendChild(textarea);
    textarea.select();
    if (document.execCommand('copy')) {
        document.execCommand('copy');
        //alert(content);
        console.log("复制成功");
    }
    document.body.removeChild(textarea);
}
function copyw4() {
    var name = '您好，您的账号需要到该【】时间段才能申请解除限制，请您等待时间到后联系客服处理！';
    var url = '';
    var form = '';
    var content = name.concat(url, form);
    // 使用textarea支持换行，使用input不支持换行
    const textarea = document.createElement('textarea');
    textarea.value = content;
    document.body.appendChild(textarea);
    textarea.select();
    if (document.execCommand('copy')) {
        document.execCommand('copy');
        //alert(content);
        console.log("复制成功");
    }
    document.body.removeChild(textarea);
}
function copyw5() {
    var name = '您的账号将于【】时间段解除限制，如若系统未自动解除限制的话，请您联系在线客服处理即可呢~';
    var url = '';
    var form = '';
    var content = name.concat(url, form);
    // 使用textarea支持换行，使用input不支持换行
    const textarea = document.createElement('textarea');
    textarea.value = content;
    document.body.appendChild(textarea);
    textarea.select();
    if (document.execCommand('copy')) {
        document.execCommand('copy');
        //alert(content);
        console.log("复制成功");
    }
    document.body.removeChild(textarea);
}
function copyw6() {
    var name = '您好，系统只判断您是否在规定时间内放币，不是说您故意或不是故意的就取消对您做出的惩罚，后续还请您在规定的时间内完成订单！';
    var url = '';
    var form = '';
    var content = name.concat(url, form);
    // 使用textarea支持换行，使用input不支持换行
    const textarea = document.createElement('textarea');
    textarea.value = content;
    document.body.appendChild(textarea);
    textarea.select();
    if (document.execCommand('copy')) {
        document.execCommand('copy');
        //alert(content);
        console.log("复制成功");
    }
    document.body.removeChild(textarea);
}
function copyw7() {
    var name = '目前客服已为您提交申请解除，还请您后续收到买家的款项后务必在规定时间内完成订单，避免后续交易中触发严重限制情况~';
    var url = '';
    var form = '';
    var content = name.concat(url, form);
    // 使用textarea支持换行，使用input不支持换行
    const textarea = document.createElement('textarea');
    textarea.value = content;
    document.body.appendChild(textarea);
    textarea.select();
    if (document.execCommand('copy')) {
        document.execCommand('copy');
        //alert(content);
        console.log("复制成功");
    }
    document.body.removeChild(textarea);
}
document.onselectstart = new Function("event.returnValue=false;");
document.oncontextmenu = function() {
    return false;
}
function copyw8() {
    var name = '您好，与mipay钱包合作的游戏平台较多，您需要咨询一下您当前娱乐的游戏平台是否支持mipay钱包的充值渠道，支持就可以使用mipay钱包买卖交易~';
    var url = '';
    var form = '';
    var content = name.concat(url, form);
    // 使用textarea支持换行，使用input不支持换行
    const textarea = document.createElement('textarea');
    textarea.value = content;
    document.body.appendChild(textarea);
    textarea.select();
    if (document.execCommand('copy')) {
        document.execCommand('copy');
        //alert(content);
        console.log("复制成功");
    }
    document.body.removeChild(textarea);
}
function copyw9() {
    var name = '由于您在未支付的情况下点击了【我已转账】系统禁用c2c交易24小时，如有疑问请联系在线客服~';
    var url = '';
    var form = '';
    var content = name.concat(url, form);
    // 使用textarea支持换行，使用input不支持换行
    const textarea = document.createElement('textarea');
    textarea.value = content;
    document.body.appendChild(textarea);
    textarea.select();
    if (document.execCommand('copy')) {
        document.execCommand('copy');
        //alert(content);
        console.log("复制成功");
    }
    document.body.removeChild(textarea);
}
function copyw10() {
    var name = '您好，由于您在未支付的情况下点击了【我已转账】并上传非支付成功的截图，因此根据用户协议，您被系统禁止购买24小时~';
    var url = '';
    var form = '';
    var content = name.concat(url, form);
    // 使用textarea支持换行，使用input不支持换行
    const textarea = document.createElement('textarea');
    textarea.value = content;
    document.body.appendChild(textarea);
    textarea.select();
    if (document.execCommand('copy')) {
        document.execCommand('copy');
        //alert(content);
        console.log("复制成功");
    }
    document.body.removeChild(textarea);
}
function copyw11() {
    var name = '还请您后续在交易购买时，请务必向卖家支付后再点击【我已转账】并上传支付成功的有效截图，否则多次违规的话，将被系统永久封禁账号的~';
    var url = '';
    var form = '';
    var content = name.concat(url, form);
    // 使用textarea支持换行，使用input不支持换行
    const textarea = document.createElement('textarea');
    textarea.value = content;
    document.body.appendChild(textarea);
    textarea.select();
    if (document.execCommand('copy')) {
        document.execCommand('copy');
        //alert(content);
        console.log("复制成功");
    }
    document.body.removeChild(textarea);
}