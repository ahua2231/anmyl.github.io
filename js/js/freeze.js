document.onselectstart = new Function("event.returnValue=false;");
document.oncontextmenu = function() {
    return false;
}
function copyw1() {
    var name = '您好，您的账号并不是因为没有达到使用年龄而被限制，目前您的账号是因为触发【00】后机制而被风控，您联系一下平台方来与mipay官方对接即可申请解除~';
    var url = '';
    var form = '';
    var content = name.concat(url, form);
    // 使用textarea支持换行，使用input不支持换行
    const textarea = document.createElement('textarea');
    textarea.value = content;
    document.body.appendChild(textarea);
    textarea.select();
    if (document.execCommand('copy')) {
        document.execCommand('copy');
        //alert(content);
        console.log("复制成功");
    }
    document.body.removeChild(textarea);
}
function copyw2() {
    var name = '您好，目前mipay设有00后使用机制，00后的需要游戏平台来与mipay官方对接后才能正常通过使用~';
    var url = '';
    var form = '';
    var content = name.concat(url, form);
    // 使用textarea支持换行，使用input不支持换行
    const textarea = document.createElement('textarea');
    textarea.value = content;
    document.body.appendChild(textarea);
    textarea.select();
    if (document.execCommand('copy')) {
        document.execCommand('copy');
        //alert(content);
        console.log("复制成功");
    }
    document.body.removeChild(textarea);
}
function copyw3() {
    var name = '您好，由于您的身份证信息（00后）使用限制，该账户通过审核需要与目前您娱乐的平台核实确定是本人使用情况下才能为您解除限制~';
    var url = '';
    var form = '';
    var content = name.concat(url, form);
    // 使用textarea支持换行，使用input不支持换行
    const textarea = document.createElement('textarea');
    textarea.value = content;
    document.body.appendChild(textarea);
    textarea.select();
    if (document.execCommand('copy')) {
        document.execCommand('copy');
        //alert(content);
        console.log("复制成功");
    }
    document.body.removeChild(textarea);
}
function copyw4() {
    var name = '您好，因系统检测到您的实名认证年龄过小，存在不是本人使用的情况，需要您娱乐的游戏平台来与mipay官方反馈是您本人使用，才能为您申请解除限制~';
    var url = '';
    var form = '';
    var content = name.concat(url, form);
    // 使用textarea支持换行，使用input不支持换行
    const textarea = document.createElement('textarea');
    textarea.value = content;
    document.body.appendChild(textarea);
    textarea.select();
    if (document.execCommand('copy')) {
        document.execCommand('copy');
        //alert(content);
        console.log("复制成功");
    }
    document.body.removeChild(textarea);
}
function copyw5() {
    var name = '麻烦您这边到娱乐平台联系一下客服，然后让对方与MiPay平台对接后这边会为您审核处理~';
    var url = '';
    var form = '';
    var content = name.concat(url, form);
    // 使用textarea支持换行，使用input不支持换行
    const textarea = document.createElement('textarea');
    textarea.value = content;
    document.body.appendChild(textarea);
    textarea.select();
    if (document.execCommand('copy')) {
        document.execCommand('copy');
        //alert(content);
        console.log("复制成功");
    }
    document.body.removeChild(textarea);
}
function copyw6() {
    var name = '若是本人使用的话，请您联系一下您娱乐的游戏平台客服，让游戏客服来与mipay官方对接核实即可~';
    var url = '';
    var form = '';
    var content = name.concat(url, form);
    // 使用textarea支持换行，使用input不支持换行
    const textarea = document.createElement('textarea');
    textarea.value = content;
    document.body.appendChild(textarea);
    textarea.select();
    if (document.execCommand('copy')) {
        document.execCommand('copy');
        //alert(content);
        console.log("复制成功");
    }
    document.body.removeChild(textarea);
}
function copyw7() {
    var name = '根据卖家反馈，该笔资金存在使用他人账户进行交易付款，MIPAY禁止使用非本人账户进行交易，该笔订单卖家已锁定请您提供相关明细证明该笔资金来源于正规渠道~';
    var url = '';
    var form = '';
    var content = name.concat(url, form);
    // 使用textarea支持换行，使用input不支持换行
    const textarea = document.createElement('textarea');
    textarea.value = content;
    document.body.appendChild(textarea);
    textarea.select();
    if (document.execCommand('copy')) {
        document.execCommand('copy');
        //alert(content);
        console.log("复制成功");
    }
    document.body.removeChild(textarea);
}
function copyw8() {
    var name = '您好，根据卖家反馈您使用非实名付款，mipay官方禁止使用非实名交易，请问一下您与转账人是什么关系，为何不用自己的付款方式付款？';
    var url = '';
    var form = '';
    var content = name.concat(url, form);
    // 使用textarea支持换行，使用input不支持换行
    const textarea = document.createElement('textarea');
    textarea.value = content;
    document.body.appendChild(textarea);
    textarea.select();
    if (document.execCommand('copy')) {
        document.execCommand('copy');
        //alert(content);
        console.log("复制成功");
    }
    document.body.removeChild(textarea);
}
function copyw9() {
    var name = '您好，核实到您这边多次非实名交易，因此官方被限制购买，客服这边可以为您申请解除，若后续再次出现非实名交易，直接取消订单需要自行承担风险，同时您的mipay账号将被永久冻结处理，请您后续交易务必遵守mipay交易规则！';
    var url = '';
    var form = '';
    var content = name.concat(url, form);
    // 使用textarea支持换行，使用input不支持换行
    const textarea = document.createElement('textarea');
    textarea.value = content;
    document.body.appendChild(textarea);
    textarea.select();
    if (document.execCommand('copy')) {
        document.execCommand('copy');
        //alert(content);
        console.log("复制成功");
    }
    document.body.removeChild(textarea);
}
function copyw10() {
    var name = '您好，您被限制12小时的【充值】权限目前已经恢复正常，至于您的【购买】权限则需等待至24小时后再来联系客服申请解除~';
    var url = '';
    var form = '';
    var content = name.concat(url, form);
    // 使用textarea支持换行，使用input不支持换行
    const textarea = document.createElement('textarea');
    textarea.value = content;
    document.body.appendChild(textarea);
    textarea.select();
    if (document.execCommand('copy')) {
        document.execCommand('copy');
        //alert(content);
        console.log("复制成功");
    }
    document.body.removeChild(textarea);
}
function copyw11() {
    var name = '您好，因您违反了MIpay钱包内用户协议，使用非实名信息交易，因此系统将您的购买权限禁止24小时！';
    var url = '';
    var form = '';
    var content = name.concat(url, form);
    // 使用textarea支持换行，使用input不支持换行
    const textarea = document.createElement('textarea');
    textarea.value = content;
    document.body.appendChild(textarea);
    textarea.select();
    if (document.execCommand('copy')) {
        document.execCommand('copy');
        //alert(content);
        console.log("复制成功");
    }
    document.body.removeChild(textarea);
}
function copyw12() {
    var name = 'MiPay钱包交易必须使用本人的实名账号进行【收付款】，否则由此产生的订单纠纷将由您承担风险，同时使用非实名转账将被系统禁止充值也购买权限！';
    var url = '';
    var form = '';
    var content = name.concat(url, form);
    // 使用textarea支持换行，使用input不支持换行
    const textarea = document.createElement('textarea');
    textarea.value = content;
    document.body.appendChild(textarea);
    textarea.select();
    if (document.execCommand('copy')) {
        document.execCommand('copy');
        //alert(content);
        console.log("复制成功");
    }
    document.body.removeChild(textarea);
}
function copyw13() {
    var name = '您好，核实到该账号存在威胁举报行为，mipay官方永久禁止使用该账号，您这边无法使用mipay钱包！';
    var url = '';
    var form = '';
    var content = name.concat(url, form);
    // 使用textarea支持换行，使用input不支持换行
    const textarea = document.createElement('textarea');
    textarea.value = content;
    document.body.appendChild(textarea);
    textarea.select();
    if (document.execCommand('copy')) {
        document.execCommand('copy');
        //alert(content);
        console.log("复制成功");
    }
    document.body.removeChild(textarea);
}
function copyw14() {
    var name = '您的账号已经无法申请解除，若客服这边没有相关的证明是不会提交系统冻结您的账号~';
    var url = '';
    var form = '';
    var content = name.concat(url, form);
    // 使用textarea支持换行，使用input不支持换行
    const textarea = document.createElement('textarea');
    textarea.value = content;
    document.body.appendChild(textarea);
    textarea.select();
    if (document.execCommand('copy')) {
        document.execCommand('copy');
        //alert(content);
        console.log("复制成功");
    }
    document.body.removeChild(textarea);
}
function copyw15() {
    var name = '您的账号不是因上传截图错误导致被冻结，是因您报警威胁卖家要求卖家退款处理才导致~';
    var url = '';
    var form = '';
    var content = name.concat(url, form);
    // 使用textarea支持换行，使用input不支持换行
    const textarea = document.createElement('textarea');
    textarea.value = content;
    document.body.appendChild(textarea);
    textarea.select();
    if (document.execCommand('copy')) {
        document.execCommand('copy');
        //alert(content);
        console.log("复制成功");
    }
    document.body.removeChild(textarea);
}
function copyw16() {
    var name = '您已经无法使用mipay钱包交易所，若您无法娱乐游戏平台，请您联系游戏客服咨询解答~';
    var url = '';
    var form = '';
    var content = name.concat(url, form);
    // 使用textarea支持换行，使用input不支持换行
    const textarea = document.createElement('textarea');
    textarea.value = content;
    document.body.appendChild(textarea);
    textarea.select();
    if (document.execCommand('copy')) {
        document.execCommand('copy');
        //alert(content);
        console.log("复制成功");
    }
    document.body.removeChild(textarea);
}
function copyw17() {
    var name = 'mipay在线客服只能解答mipay钱包上的问题，无法为您处理您游戏平台的问题~';
    var url = '';
    var form = '';
    var content = name.concat(url, form);
    // 使用textarea支持换行，使用input不支持换行
    const textarea = document.createElement('textarea');
    textarea.value = content;
    document.body.appendChild(textarea);
    textarea.select();
    if (document.execCommand('copy')) {
        document.execCommand('copy');
        //alert(content);
        console.log("复制成功");
    }
    document.body.removeChild(textarea);
}
function copyw18() {
    var name = '抱歉，该账号已经永久冻结，客服是无法帮助到您，永久冻结账户无法申请解除限制，请您知晓！';
    var url = '';
    var form = '';
    var content = name.concat(url, form);
    // 使用textarea支持换行，使用input不支持换行
    const textarea = document.createElement('textarea');
    textarea.value = content;
    document.body.appendChild(textarea);
    textarea.select();
    if (document.execCommand('copy')) {
        document.execCommand('copy');
        //alert(content);
        console.log("复制成功");
    }
    document.body.removeChild(textarea);
}
function copyw19() {
    var name = '您好，您在使用mipay钱包时遇到什么问题，需要申请永久冻结您的账号，还请您告知一下！';
    var url = '';
    var form = '';
    var content = name.concat(url, form);
    // 使用textarea支持换行，使用input不支持换行
    const textarea = document.createElement('textarea');
    textarea.value = content;
    document.body.appendChild(textarea);
    textarea.select();
    if (document.execCommand('copy')) {
        document.execCommand('copy');
        //alert(content);
        console.log("复制成功");
    }
    document.body.removeChild(textarea);
}
function copyw20() {
    var name = '您好，已注册的账号是无法注销，若您长期不登录系统就会开启保护机制将您的账号冻结，您不来申请是无法自动解除~';
    var url = '';
    var form = '';
    var content = name.concat(url, form);
    // 使用textarea支持换行，使用input不支持换行
    const textarea = document.createElement('textarea');
    textarea.value = content;
    document.body.appendChild(textarea);
    textarea.select();
    if (document.execCommand('copy')) {
        document.execCommand('copy');
        //alert(content);
        console.log("复制成功");
    }
    document.body.removeChild(textarea);
}
function copyw21() {
    var name = '您好，mipay钱包是没有注销功能，客服这边也告知您，若您长期未登录系统会开启保护机制将您的账号冻结，您需要使用的时候再来申请解除！';
    var url = '';
    var form = '';
    var content = name.concat(url, form);
    // 使用textarea支持换行，使用input不支持换行
    const textarea = document.createElement('textarea');
    textarea.value = content;
    document.body.appendChild(textarea);
    textarea.select();
    if (document.execCommand('copy')) {
        document.execCommand('copy');
        //alert(content);
        console.log("复制成功");
    }
    document.body.removeChild(textarea);
}